{
  "type": "object",
  "additionalProperties": false,
  "required": ["userId", "name", "email", "balance"],
  "properties": {
    "userId": { "type": "string", "minLength": 1, "maxLength": 100 },
    "name": { "type": "string", "minLength": 1, "maxLength": 100 },
    "email": { "type": "string", "format": "email" },
    "balance": { "type": "number" }
  }
}
